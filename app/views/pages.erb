<% @pages.each do |p| %>
  <a href="/page/<%= p.id %>">
    <div class="page_title"><%= p.title %> : <%= p.user.username %> : <%= p.created_on %></div> 
  </a>
<% end %>

<br>
<a href="/new_page">New Page</a>